{
  "name": "rxxldq-private-insights",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "test": "node --test test/*.test.mjs",
    "db:local": "wrangler d1 migrations apply rxxldq-insights --local",
    "db:remote": "wrangler d1 migrations apply rxxldq-insights --remote"
  },
  "devDependencies": {
    "@types/node": "^26.3.0",
    "wrangler": "4.126.0"
  }
}
